import 'package:flutter/material.dart';

class MusicSuggestion extends StatelessWidget {
  final Widget _widget;
  const MusicSuggestion(this._widget, {Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
